# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
*_credentials*.json
service-account*.json

# Debug and test artifacts (don't commit)
*.log
logs/
debug_*.json
TEST_RESULTS.json
transcriptions/

# PIEZA 19 — artefactos generados por tests/e2e/ (HTML del Brand Soul y
# screenshots) para que el CEO los abra localmente. No son codigo.
tests/e2e/_salida/

# Python
__pycache__/
*.py[cod]
*.so
.Python
build/
dist/
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
npm-debug.log*
.next/
out/

# Media work files
*.mp4
*.mov
*.wav
*.mp3
*.psd
*.pptx
renders/
!docs/**/*.png

# OS and IDE
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp

# Internal strategy — this repo ships code only.
# Specs, competitive analysis and hackathon planning live outside it.
.claude/
specs/
*_RESPUESTAS.json
spec_respuestas.json
SUBMISSION.json
PITCH_SOURCE.md

# Working notes from build sessions — scratch, not documentation.
# They duplicate the piezas in .claude/specs/ and rot fast. The repo keeps
# README.md, QUICK_START.md and docs/ ; everything else stays local.
PIEZA_*.md
PHYHANDOFF_*.md
MODO_A_*.md
FIX_SUMMARY.md
AUDIO_PERMISSION_ISSUE.md
PROGRESS.md
PROPUESTA_PRODUCCION.md

# 2026-09-21 — artefactos que se colaron a main (repo PUBLICO). El carril
# paralelo de MiniMax comitea con `git add -A`, asi que esto es lo unico que
# los frena. Nada de esto es codigo: salida de tests, scripts de debug y notas.
cache/
pytest-of-*/
.port_used
test_screenshot.png
# scripts de debug dentro de static/: se servian publicos en /static/*.py
app/static/*.py
debug_*.py
fix_*.js
test_*_local.py
*.bat
chrome-devtools-mcp/
chrome*mcp*.md
package-lock.json
# notas y QA sueltos en la raiz -- la politica del repo es "ships code only"
/*_QA.html
/QA_CHECKLIST.html
/PLAN_DE_PRUEBA_*.html
/SUMMARY.md
/PROYECTO_COMPLETO.md
/DEPLOYMENT_CHECKLIST.md
/SECURITY_AUDIT_REPORT.md
# carpeta anidada con los analisis de MiniMax (viven en disco, no en el repo)
/brand-studio-agent/
